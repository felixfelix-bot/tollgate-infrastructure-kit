---
- name: Deploy cost probe + sanitizer
  hosts: localhost
  connection: local
  become: false
  gather_facts: yes
  environment:
    XDG_RUNTIME_DIR: "/run/user/{{ lookup('pipe', 'id -u') }}"
  roles:
    - hermes_cost_probe
